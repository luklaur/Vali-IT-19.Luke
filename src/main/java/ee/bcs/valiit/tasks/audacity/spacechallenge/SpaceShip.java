package ee.bcs.valiit.tasks.audacity.spacechallenge;


// 1. Create a SpaceShip Interface that includes the definitions of these methods:

public interface SpaceShip<launch> {

   // launch: a method that returns either true or
    //  false indicating if the launch was successful
    //  or if the rocket has crashed.

   // land: a method that also returns either
    // true or false based on the success of the landing.


   // canCarry: a method that takes an Item as an
    // argument and returns true if the rocket can
    // carry such item or false if it will exceed
    // the weight limit.





}
